I want a web-based BINGO game. It should ideally be implemented in javascript and run entirely in the browser, not relying on any server-side technologies. It should work on both desktop and mobile browsers transparently. It should render a 5x5 grid of rectangles on the user's screen, and should randomly place one line from @squares.txt into each rectangle. The exception is the 3rd square in the 3rd row, the middle square, which should say FREE SQUARE. The process of populating the grid should be animated, and the text should pop into the rectangles in a random order. The process of animating the creation of the card should happen automatically on page load, and should have a duration of 7 seconds. When the user clicks on a rectangle, a large X should be drawn covering that rectangle only. If the user clicks the rectangle again, the X should be removed. Go
